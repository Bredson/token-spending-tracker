{
  "name": "token-spending-tracker",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "watch": "npm run watch --workspaces --if-present"
  }
}
